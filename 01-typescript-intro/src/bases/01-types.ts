export const name = 'juan';
export const age: number = 35;
export const isValid: boolean = true;

export const templateString = ` Esto es un string multolonea 
que puede tener "dobles
' simple 
 inyectar valores ${isValid}`;
